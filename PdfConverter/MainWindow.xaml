<Window x:Class="PdfConverter.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:my="clr-namespace:FileInputBox;assembly=FileInputConsumerBrowseBox"

        xmlns:local="clr-namespace:PdfConverter" d:DataContext="{d:DesignInstance Type=local:ApplicationViewModel}"
        mc:Ignorable="d"
        Title="PDF Converter" Height="400" Width="900">
    <Grid >

        <StackPanel Grid.Column="1" >
            <StackPanel Grid.Column="1" DataContext="{Binding Certificate}">
                <TextBlock Text="Фамилия Имя Отчество" Margin="20,20,0,0"/>
                <TextBox Text="{Binding Fio, UpdateSourceTrigger=PropertyChanged,  NotifyOnValidationError=True, ValidatesOnDataErrors=True}" Margin="20,0,10,10" Height="32" FontStretch="Expanded">
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}">
                            <Setter Property="Validation.ErrorTemplate">
                                <Setter.Value>
                                    <ControlTemplate>
                                        <DockPanel LastChildFill="True">
                                            <TextBlock DockPanel.Dock="Right" Foreground="Red" FontSize="14" FontWeight="Bold"
                                                       Text="*"/>
                                            <Border BorderBrush="Red" BorderThickness="2">
                                                <AdornedElementPlaceholder Name="adornerPlaceholder"/>
                                            </Border>
                                        </DockPanel>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="Validation.HasError" Value="True">
                                    <Setter Property="Foreground" Value="Red"/>
                                    <Setter Property="ToolTip" 
                         Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <TextBlock Text="Номер сертификата" Margin="20,0"/>
                <TextBox Text="{Binding Fingerprint, UpdateSourceTrigger=PropertyChanged,  NotifyOnValidationError=True, ValidatesOnDataErrors=True}" Margin="20,0,10,10" Height="32" FontStretch="Expanded">
                <TextBox.Style>
                    <Style TargetType="{x:Type TextBox}">
                        <Setter Property="Validation.ErrorTemplate">
                            <Setter.Value>
                                <ControlTemplate>
                                    <DockPanel LastChildFill="True">
                                        <TextBlock DockPanel.Dock="Right" Foreground="Red" FontSize="14" FontWeight="Bold"
                                                       Text="*"/>
                                        <Border BorderBrush="Red" BorderThickness="2">
                                            <AdornedElementPlaceholder Name="adornerPlaceholder"/>
                                        </Border>
                                    </DockPanel>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="Validation.HasError" Value="True">
                                <Setter Property="Foreground" Value="Red"/>
                                <Setter Property="ToolTip" 
                         Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TextBox.Style>
                </TextBox>
                    <StackPanel Orientation="Horizontal" >
                    <TextBlock Text="Срок действия сертификата" Margin="20,0,10,0" VerticalAlignment="Center"/>
                    <TextBlock Text="  c " HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0,0,0"></TextBlock>
                    <DatePicker SelectedDate ="{Binding DateFrom, StringFormat=dd/MM/yyyy, UpdateSourceTrigger=PropertyChanged,  NotifyOnValidationError=True, ValidatesOnDataErrors=True}" FirstDayOfWeek="Monday" Margin="0,0,10,0">
                        <DatePicker.Style>
                            <Style TargetType="{x:Type DatePicker}">
                                <Setter Property="Validation.ErrorTemplate">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <DockPanel LastChildFill="True">
                                                <TextBlock DockPanel.Dock="Right" Foreground="Red" FontSize="14" FontWeight="Bold"
                                                       Text="*"/>
                                                <Border BorderBrush="Red" BorderThickness="2">
                                                    <AdornedElementPlaceholder Name="adornerPlaceholder"/>
                                                </Border>
                                            </DockPanel>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="Validation.HasError" Value="True">
                                        <Setter Property="Foreground" Value="Red"/>
                                        <Setter Property="ToolTip" 
                         Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DatePicker.Style>
                    </DatePicker>
                    <TextBlock Text="  по " HorizontalAlignment="Right" VerticalAlignment="Center" Margin="20,0,0,0"/>
                    <DatePicker SelectedDate ="{Binding DateTo, StringFormat=dd/MM/yyyy, UpdateSourceTrigger=PropertyChanged,  NotifyOnValidationError=True, ValidatesOnDataErrors=True}" FirstDayOfWeek="Monday" Margin="0,0,10,0">
                        <DatePicker.Style>
                            <Style TargetType="{x:Type DatePicker}">
                                <Setter Property="Validation.ErrorTemplate">
                                    <Setter.Value>
                                        <ControlTemplate>
                                            <DockPanel LastChildFill="True">
                                                <TextBlock DockPanel.Dock="Right" Foreground="Red" FontSize="14" FontWeight="Bold"
                                                       Text="*"/>
                                                <Border BorderBrush="Red" BorderThickness="2">
                                                    <AdornedElementPlaceholder Name="adornerPlaceholder"/>
                                                </Border>
                                            </DockPanel>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="Validation.HasError" Value="True">
                                        <Setter Property="Foreground" Value="Red"/>
                                        <Setter Property="ToolTip" 
                         Value="{Binding RelativeSource={RelativeSource Self}, Path=(Validation.Errors)[0].ErrorContent}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </DatePicker.Style>
                    </DatePicker>
                </StackPanel>
            </StackPanel>
            <TextBlock Text="Путь к файлу" Margin="20,20,0,0"/>
            <my:FileInputBrowseBox DataContext="{Binding Certificate}" FileName="{Binding FilePath ,Mode=TwoWay, UpdateSourceTrigger=PropertyChanged,  NotifyOnValidationError=True, ValidatesOnDataErrors=True}"  Margin="20,0,10,10"/>
            <Grid Margin="20">
                <ProgressBar Minimum="0" Maximum="100" Value="{Binding Progress, Mode=TwoWay}" Name="pbStatus" Height="30">
                    <ProgressBar.Style>
                        <Style TargetType="ProgressBar">
                            <Setter Property="Foreground" Value="Blue" />
                            <Setter Property="Background" Value="LightGray"/>
                            <Style.Triggers>
                                <Trigger Value="0" Property="Value">
                                    <Setter Property="Background" Value="White"></Setter>
                                </Trigger>
                                <Trigger Value="100" Property="Value">
                                    <Setter Property="Foreground" Value="Lime"></Setter>
                                </Trigger>
                                <DataTrigger Value="True" Binding="{Binding ProcessError}">
                                    <Setter Property="Foreground" Value="Red" />
                                </DataTrigger>
                                
                            </Style.Triggers>
                        </Style>
                    </ProgressBar.Style>
                </ProgressBar>
                <TextBlock Text="{Binding ProcessStage, Mode=TwoWay, StringFormat = Стадия: {0}}"  HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
        </StackPanel>
        
        <Button Height="32"  Command="{Binding ConvertCommand}" VerticalAlignment="Bottom" >Преобразовать</Button>
    </Grid>

</Window>
